<h1><%= @album.title %></h1>
<p><%= @album.description %></p>

<h3>Photos</h3>

<%= render partial: "shared/image_grid", locals: {posts: @album.posts} %>

<div class="row"></div>
<% if @album.user == current_user %>
  <%= link_to "Edit Album", [:edit, @album], class: "btn btn-default" %>
  <%= button_to "Delete Album", @album, method: :delete, form_class: "inline-block", class: "btn btn-danger" %>
<% end %>
